--------
todos: TODOs
--------

Input mechanisms:
    STDIN
    FILE
    CLIP

Input structure:
    paragraphs
    pairs
    rows

--------
road-map: Road map
--------

Document useful Path components:
    p = Path('/foo/bar/fubb.txt')
    p.parent    /foo/bar
    p.name      fubb.txt
    p.suffix    .txt
    p.stem      fubb

Sequence numbering.

Logging.
Log inquiries.

Case-change-only renamings.
Remove common prefix/suffix.

Other operations: copy, backup, link, chmod, touch.

Renaming via library use case.

Option: --row-delimiter [default: tab]
Option: --path-delimiter [default: newline]
Option: --no-strip [default: paths are stripped]
Validation overrides: clobber, skip-equal, create-dirs.

Other stuff:
    Undo.
    Alternative dryrun output styles: should support the same behavior as input structures.
    Complex failure/validation scenarios: prior renaming would make subsequent renaming fail [eg, changing directory path before attempting a file rename]
    Old-path filtering: files-only, dirs-only.

